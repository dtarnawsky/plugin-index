{
  "type": "module",
  "scripts": {
    "start": "npx tsc && node out/index.js all",
    "build": "npm run start",    
    "publish": "cp data/index.html dist && netlify deploy --dir dist --prod"
  },
  "devDependencies": {
    "@types/node": "18.15.12",
    "typescript": "^5.0.4"
  },
  "dependencies": {
    "node-fetch": "^3.3.1"
  }
}
